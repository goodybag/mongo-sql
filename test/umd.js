/**
 * Turns out, I think we'll have to use something like phantom to
 * properly test that this works. Womp womp. We'll do it later
 */

// var assert    = require('assert');
// var requirejs = require('requirejs');

// describe('UMD Compatibility', function(){

//   it('requirejs should load the module', function(done){
//     requirejs([ __dirname + '/../index'], function(builder){
//       console.log("Got", __dirname + '/../index', builder)
//       assert.equal( !!builder.sql, true )
//       done();
//     });
//   });

// });